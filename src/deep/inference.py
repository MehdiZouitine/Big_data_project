import torch
import numpy as np
from tqdm import tqdm
import torch.nn as nn
from torch.utils.data import DataLoader


def make_submission(model: nn.Module, model_path: str, dataloader: Dataloader):
    pass
